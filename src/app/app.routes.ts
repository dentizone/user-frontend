import { HomeContainerComponent } from './features/Home/home-container.component';
import { Routes } from '@angular/router';

export const routes: Routes = [{ path: '', component: HomeContainerComponent }];
